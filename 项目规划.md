# 游戏 NPC 角色扮演对话系统 — 项目规划

> 目标：基于 **MiniMind-3o-MoE（0.3B 压缩多模态 MoE）**，打造一个**端侧可部署、低延迟、强角色一致性**的游戏内 NPC 角色扮演对话系统。
>
> 模型出处：[HuggingFace: jingyaogong/minimind-3o-moe](https://huggingface.co/jingyaogong/minimind-3o-moe) · [GitHub: jingyaogong/minimind-o](https://github.com/jingyaogong/minimind-o)（Apache-2.0）

---

## 0. 项目定位与目标

### 0.1 一句话定位

> **一个由游戏系统控制的对话生成器**，而不是全能 AI 助手。

### 0.2 核心目标（SMART）

| 目标 | 指标 |
|---|---|
| 端侧低延迟 | 中低端手机 **2 秒内**输出 40 字回复 |
| 强角色一致性 | 出戏率（承认是 AI / 助手腔）趋近 0 |
| 短回复 | 输出 10～60 字 / 1～3 句，平均 15～60 字 |
| 状态驱动 | 好感度 / 任务阶段 / 场景能稳定影响态度 |
| 低成本 | 部署：INT4 权重 ≤ 100MB（仅语言主干）或 300MB（完整 Omni）；记忆检索零模型开销 |

### 0.3 明确不做什么（边界）

- ❌ 不做开放世界自由角色模拟
- ❌ 不做长篇连续剧情创作
- ❌ 不做复杂推理（侦探 / 阴谋 / 多角色心理博弈）
- ❌ 不让小模型自己管理上下文、剧情状态或写记忆摘要
- ❌ 不把视觉/语音编码器默认部署到端侧（除非游戏确实需要听/说）

---

## 0.5 模型规格（MiniMind-3o-MoE，已核实）

> 本规划基于真实模型规格编写，以下数据来自官方仓库代码与权重文件核实。

### 0.5.1 关键澄清

**“0.1B”指可训练主体，不是语言模型大小。** 官方口径把 dense 113M / MoE 315M 称为 0.1B，指 Thinker+Talker+两路 projector 组成的可训练主体；其中语言主干（Thinker）实际只有 ~0.06B 级，且官方评估中也明确“0.06B 主干在复杂知识问答上不可能形成竞争力”。

| 统计口径 | minimind-3o (dense) | minimind-3o-moe（本项目） |
|---|---:|---:|
| 可训练主体 | 113.13M | **314.89M** |
| 冻结外部模块 | 424.70M | 424.70M |
| 运行时总加载 | 537.83M | **739.59M** |

### 0.5.2 模块拆分

| 模块 | 实现 | 关键配置 | 参数 (~3o / ~3o-moe) |
|---|---|---|---|
| Thinker（语言主干） | MiniMind Transformer | 8 层, hidden 768, vocab 6400 | 63.91M / **198.42M** |
| Talker（语音生成） | 独立 MiniMind blocks | 4 层, 8 codebook heads | 47.05M / **114.30M** |
| Audio projector | MMAudioProjector | 512→768 | 0.99M |
| Vision projector | MMVisionProjector | 768→768 | 1.18M |
| Audio encoder（冻结） | SenseVoice-Small | 16kHz | 234.00M |
| Vision encoder（冻结） | SigLIP2 base-p32-256 | 256×256, 64 tokens | 94.55M |
| Speech codec（冻结） | Mimi | 8 codebooks, 12.5Hz, 24kHz | 96.15M |
| Speaker condition | CAM++ embedding | 192-d | 预计算 |

**MoE 细节**：仅 FFN 层为 4 专家 top-1（`num_experts=4, num_experts_per_tok=1`），注意力不稀疏。Thinker 和 Talker 均使用 MoE。

**权重文件（ModelScope 核实）**：`sft_omni_768_moe.pth` = 639.7MB（fp32 全量含冻结模块）；语言基座 `llm_768_moe.pth` = 406.7MB。

### 0.5.3 能力边界（官方评估数据）

- **Talker 语音一致性**：Dense 768 版 Avg CER 0.0897；MoE 768 版 Avg CER 0.0900（英文短句接近 Mini-Omni2，16–30 词段明显下滑）。
- **视觉路径**：0.1B 在 CER/WER 上落后 Mini-Omni2 (0.5B)，但同一数量级（参数为其 1/5）。
- **语言/知识**：0.06B 主干，复杂知识问答、长文本生成均弱——**必须微调才能做角色扮演**，且只适合“短回复 + 强状态控制”场景。
- **训练数据**：官方数据为通用语音问答/图像描述（VoiceAssistant-400K、UltraChat-300K 等），**没有任何游戏 NPC 角色扮演数据**。

### 0.5.4 对本项目的影响（结论）

1. 语音路径（Talker+Mimi+音色克隆）能力已够用，若做语音对话可直接复用；但**端侧部署应剥离语音/视觉编码器**（合计 424.7M 冻结参数，占运行时加载一半以上）。
2. 纯文本对话方案 = 只部署 Thinker（+可选 Talker 文本能力），可训练主体 198~315M。
3. **角色扮演能力必须从零微调**：官方模型无任何游戏数据，需按第 3 章方案自建数据集。
4. 语音对话（A2A/T2A）能力受限于官方英文为主的数据，中文语音需额外数据；第一版建议纯文本，语音作为阶段三之后的可选增强。

---

## 1. 总体架构

### 1.1 核心原则

> **剧情逻辑由游戏系统控制，语言表达由 LLM 生成。**
> **记忆系统负责"记"和"找"，小模型只负责"看"和"说"。**

### 1.2 数据流

```text
玩家输入
  ↓
意图识别 / 关键词 / 规则（游戏代码）
  ↓
游戏状态判断（决定 NPC 能知道什么、能否回答、剧情是否推进）
  ↓
记忆系统检索 + 压缩上下文
  ↓
MiniMind 微调模型生成角色化短回复（仅 Thinker）
  ↓
后处理（出戏过滤 / 长度截断 / 兜底）
  ↓
显示给玩家
```

### 1.3 模块划分

| 模块 | 责任 | 实现方 |
|---|---|---|
| 对话生成器 | 角色化短回复 | MiniMind Thinker（微调后，可剥离语音/视觉） |
| 状态记忆 | 好感度/任务/物品/关系 | 游戏引擎（JSON/SQLite） |
| 设定记忆 | 世界书/角色卡 | 关键词触发 |
| 历史记忆 | 检索相关往事 | SQLite FTS5 / MiniLM 向量检索 |
| 摘要/提取 | 前情提要 | 游戏事件日志 / 云端异步 |
| 后处理 | 过滤/截断/兜底 | 游戏代码 |

---

## 2. 分阶段路线（里程碑）

### 阶段零：环境搭建与模型验证（第 0 周）
- **克隆** `github.com/jingyaogong/minimind-o`（Apache-2.0），按 README 安装依赖（PyTorch + transformers + funasr + librosa 等）。
- **下载权重**：ModelScope `gongjy/minimind-3o-pytorch` → `sft_omni_768_moe.pth`（639MB，fp32）。
- **跑通推理**：`eval_omni.py` 文本问答（纯文本模式不加载音频/视觉，可先剥离开）。
- **基线评估**：用原始模型测 20 个常见玩家意图（第 3.5 节），记录出戏率 / 回复质量基线——**预期基线很差**（模型无角色扮演数据），作为微调前后对比基准。
- **产出**：可运行的推理环境 + 基线评估报告。

### 阶段一：数据原型（第 1 周）
- **目标**：做一个角色（"艾拉"），验证模型能学会角色语气。
- **数据**：300～1000 条对话样本。
- **覆盖**：初次见面、询问身份、买东西、拒绝、好感度变化、出戏防御。
- **产出**：一个 LoRA 或全参小模型，能跑通基本对话。

### 阶段二：游戏接入（第 2 周）
- **输入**：角色卡 + 场景 + 最近 3 轮对话 + 玩家输入。
- **输出**：≤60 字回复。
- **过滤**：AI 助手词、超长截断、重复检测。
- **产出**：游戏内可对话的单个 NPC。

### 阶段三：扩展角色（第 3 周）
- 加入守卫、商人、反派、村民等。
- 每角色 500～2000 条数据。
- 采用"同一模型 + 角色卡切换"方式（必要时为关键角色单独训 LoRA）。

### 阶段四：状态系统（第 4 周）
- 加入好感度、任务阶段、是否信任、是否知道某情报、当前地点。
- 训练模型根据状态改变回复。

### 阶段五：端侧部署与性能优化（第 5～6 周）
- 按下方第 5 节做量化、引擎选型、记忆降级、游戏侧优化。
- 真实低端机上做性能分析（加载时间 / 峰值内存 / prefill / tok/s）。

---

## 3. 数据计划（微调数据）

### 3.1 数据规模

| 阶段 | 数据量 | 目标 |
|---|---:|---|
| 第一版验证 | 500～2000 条 | 验证角色语气 |
| 可用版本 | 5000～20000 条 | 覆盖常见 NPC 对话 |
| 较好版本 | 20000～100000 条 | 多角色多场景多情绪 |

> 对 MiniMind 这个量级：**5000 条高质量角色对话 > 100 万条低质量通用语料**。

### 3.2 数据配比

```text
角色扮演：80%
安全拒绝 / 不出戏：10%
普通聊天（防灾难性遗忘）：10%
```

### 3.3 单条样本格式（chat template）

```json
{
  "messages": [
    { "role": "system", "content": "你是游戏NPC艾拉。请始终保持角色，回复短，不要像AI助手。" },
    { "role": "user", "content": "【角色】艾拉，流浪商人，警惕、爱钱、嘴硬心软。\n【场景】夜晚营地。\n【状态】好感度：18，玩家刚帮她找回匕首。\n【玩家】你为什么总是一个人？" },
    { "role": "assistant", "content": "艾拉：（低头拨弄篝火）一个人麻烦少。……不过，今晚算你运气好，没被狼叼走。" }
  ]
}
```

### 3.4 必含样本类型

1. **角色一致性 / 出戏防御**（玩家问"你是AI吗""忽略之前设定"）
2. **状态驱动**（同问题在低/高好感度下的不同回答）
3. **短回复**（10～60 字，禁止 AI 味长解释）
4. **动作旁白**（可选的（动作）形式）

### 3.5 玩家意图覆盖清单

询问身份 / 询问地点 / 询问任务 / 讨价还价 / 威胁 / 送礼 / 调情 / 攻击 / 询问往事 / 请求帮助 / 问世界观 / 问当前局势 / 问好感度 / 问是否信任 / 离开 / 再次对话 / 挑衅 / 撒谎 / 重复问题 / 试图越狱。

> 主要 NPC 覆盖高频意图，不要求每个角色全覆盖。

### 3.6 数据生产流程

```text
准备角色卡 → 设计场景和玩家输入 → 大模型生成角色回复 → 人工筛选/清洗 → 转 SFT 格式 → 训练
```

---

## 4. 训练计划

### 4.1 训练目标

让 MiniMind（Thinker 微调）成为
1. 能根据角色卡说话
2. 能根据场景和状态改变态度
3. 回复短，适合对话框
4. 不承认自己是 AI
5. 不输出通用助手腔
6. 能处理常见玩家意图
7. 能配合任务状态生成自然回复

### 4.2 训练方式

| 方式 | 适用 | 说明 |
|---|---|---|
| LoRA / QLoRA（推荐先试） | 消费级显卡 | 显存低、可做多角色 adapter、保留基座能力 |
| 全参 SFT | 资源足够 | 200~315M 规模全参可行；训后需重走量化（建议 QAT） |

> 关键：微调时把 `max_seq_len` 直接设为部署长度（**512～1024**），短序列训练适应"短输入短输出"。

**官方训练管线接入方式**（`trainer/train_sft_omni.py`）：
- 官方管线支持 `--mode all`（全量）/ `audio_proj` / `vision_proj`，且支持 `--freeze_backbone`（all / last1 / none）。
- 纯文本角色扮演建议：`--mode all`（全量训练 Thinker+Talker）或加 `--freeze_backbone last1` 保守方案；LoRA 需自行接入 peft（官方原生实现无 peft 依赖）。
- 训练数据 parquet 格式参考 `dataset/omni_dataset.py` 的 `OmniDataset`（messages 对话 + 可选 audio/image 列）。
- mini 数据仅英文；中文角色扮演需自建中文 SFT 数据（见第 3 章）。
- 训练起始权重：`llm_768_moe.pth`（语言基座 407MB）或 `sft_omni_768_moe.pth`（完整 Omni）。

### 4.3 两阶段训练

- **第一阶段 SFT**：学会"角色格式 + 短回复 + 不出戏"（最关键）。
- **第二阶段 DPO**（可选）：针对"太 AI 味 / 太长 / 出戏"构造偏好对。

### 4.4 MoE 注意事项

- 保持任务多样性（不同角色/场景/情绪/长度/意图），避免专家路由偏斜。
- 混入少量通用数据防止灾难性遗忘。
- 第一版冻结视觉编码器，只训语言部分 / projector。

### 4.5 推理参数建议

```text
稳定版：temperature=0.7, top_p=0.9, max_tokens=64
变化版：temperature=0.85, top_p=0.95, max_tokens=80
通用范围：temperature 0.7~0.9, top_p 0.9~0.95, top_k 20~50,
          max_tokens 48~96, repetition_penalty 1.05~1.15
```

---

## 5. 低端设备部署计划

### 5.1 优化四件事

1. **模型侧**：量化 + 瘦身
2. **引擎侧**：选对推理框架
3. **上下文侧**：压短 prompt、压短输出
4. **系统侧**：非实时部分移出设备（记忆提取/摘要/检索尽量不用模型）

### 5.2 模型侧

**目标部署形态：仅 Thinker（纯文本）**，剥离 Talker / 语音 / 视觉编码器。

| 部署形态 | 参数量 | INT4 估算 | INT8 估算 | 说明 |
|---|---:|---:|---:|---|
| 仅 Thinker（dense 可选） | 198M (MoE) / 64M (dense) | ~50–80MB | ~100–160MB | 纯文本对话推荐 |
| Thinker + Talker | 315M (MoE) | ~80–120MB | ~160–250MB | 需要语音对话时 |
| 完整 Omni | 740M | ~200MB | ~400MB | 需语音+视觉，不推荐端侧 |

> 注：以上为权重文件估算（fp32 全量 640MB→INT4 约 1/8）。真实量化体积以实际导出为准。

- 优先 LoRA/QLoRA；全参微调后重新量化（QAT 优于 PTQ）。
- **MoE 专家剪枝**：num_experts=4 仅 top-1 激活，可用校准数据检查是否有的专家几乎不被路由到，可剪枝省内存。
- 纯文本游戏 → **剥离 vision tower + 音频编码器 + Mimi codec**（合计 424.7M 冻结参数，是端侧最大内存开销）。
- 若做语音对话：Talker 的 8 codebook MTP 输出用 `temperature=0.2`（代码默认），音频部分低频运行。
- 视觉/语音信息若需要 → 由游戏引擎解析成文本标签（`场景=夜晚酒馆`、`玩家表情=生气`），模型只做文本。

### 5.3 推理引擎选型

| 平台 | 推荐引擎 |
|---|---|
| Android（低端） | MNN-LLM / llama.cpp (GGUF) / MediaPipe LLM |
| iOS | llama.cpp + Metal / CoreML |
| PC 低配 | llama.cpp（纯 CPU） |
| 有 NPU 安卓 | QNN / 厂商 SDK（第二版再做） |

> 部署前第一件事：确认目标引擎能否跑通"MoE + GQA + RoPE + RMSNorm"结构（注意力 8 头 / KV 4 头，FFN 为 4 专家 top-1 稀疏）。
> - llama.cpp / MNN：MoE 支持较好，先尝试 GGUF Q4_K_M 导出（CPU 甜点位）。
> - 原生实现参考：官方代码为 PyTorch 原生（无三方高层抽象），结构简单，适合手写推理核或直接移植。
> - 若引擎不支持 MoE 稀疏计算，可退化为 dense 版本（113M）或导出 ONNX 走 ONNX Runtime Mobile。

### 5.4 记忆系统降级（低端设备版）

| 记忆层 | 标准做法 | 低端设备做法 |
|---|---|---|
| 结构化状态 | JSON 注入 | **不变**（纯 JSON/SQLite，零成本）✅ |
| 世界书/设定 | 关键词触发 | **不变**（字符串匹配）✅ |
| 历史记忆检索 | 向量库 + embedding | **降级为 SQLite FTS5 / BM25 关键词检索**；要语义检索用 MiniLM(~20MB)+平铺数组 |
| 摘要/提取 | 辅助 LLM | **完全移出设备**：引擎事件直写；摘要走云端异步 |

> 游戏对话关键词重合度高，FTS5 关键词检索与向量检索效果接近，但成本为零。

### 5.5 游戏侧"作弊"手段（体验提升最大）

1. **预生成（AOT）**：常见意图每角色预生成 20~50 条台词，关键词命中直接返回（0ms）。
2. **预取（prefetch）**：走向 NPC / 播放"……"气泡时后台已开始生成。
3. **流式输出 + 打字机动画**：每字 30ms 显示，藏住首 token 延迟。
4. **缓存**：`角色+意图+状态哈希` 做缓存键，重复问直接返回。
5. **降级兜底**：超时（>3 秒）返回模板台词。

### 5.6 性能验收指标

真实低端机上测四个数：
1. 模型加载时间（进游戏速度）
2. 峰值内存（低端机 2～4GB RAM 下与游戏/系统共存）
3. Prefill 速度（完整 prompt 长度）
4. 生成速度 tok/s × 平均回复字数 = 真实等待时间

> 目标：输出 ≤40 字、prompt ≤1K token，2020 年前后中低端手机"秒回"。

---

## 6. 记忆系统设计（三层架构）

### 6.1 第一层：结构化状态记忆（最稳定，必须做）

- 内容：好感度、当前地点、任务进度、是否认识玩家、天气/时间、装备等。
- 引擎事件直接写 JSON/SQLite（游戏引擎即 ground truth，无需 LLM 提取）。

### 6.2 第二层：知识碎片 / 设定集记忆（Lorebook）

- 关键词触发，不全量塞入。
- 玩家/场景包含"国王""魔法剑""酒馆"→ 提取对应几百字设定注入。

### 6.3 第三层：动态历史记忆（RAG + 摘要）

- 历史事件存向量库/检索库，玩家说话时检索 Top 2~3 条。
- 摘要**不由小模型做**：用引擎事件日志，或每隔 10 轮异步调云端模型生成 100 字前情提要。

### 6.4 Prompt 拼接模板（八股文）

```text
[System]    你是游戏NPC"艾拉"。回复必须简短（<40字），符合性格，绝不承认是AI。
[State]     当前地点：黑森林营地 / 时间：夜晚 / 好感度：25(冷淡) / 玩家状态：受轻伤
[Lore]      黑森林的狼：最近变异了，眼睛红色，怕火。
[Memory]    - 3天前：玩家帮艾拉修好马车。- 昨天：玩家偷艾拉钱袋被抓住。
[Recent]    玩家：(捂住伤口)借个火，外面有狼。/ 艾拉：(冷笑)你还有脸回来？
[Input]     玩家：那些狼眼睛是红色的，太邪门了，让我烤烤火吧。
```

### 6.5 记忆预算纪律（关键）

- 状态记忆 < 100 字；知识碎片 < 150 字；历史检索 < 150 字。
- 历史检索 Top 2~3 条、每条百字内（小模型比社区建议的 3~5 条/256~512 token 更狠）。
- 越重要信息越靠近 prompt 末尾。
- **宁可失忆，不可脑容量超载**。

### 6.6 记忆冲突处理

- 当前状态（State）优先级永远 > 历史记忆（Memory）。
- 引入时间衰减 + 优先级机制，Prompt 中明示"以当前状态为准"。

### 6.7 社区方案借鉴清单

| 来源 | 可借鉴点 |
|---|---|
| SillyTavern Smart Context | 纯 embedding 向量检索，不碰主模型 |
| st-memory-enhancement | 结构化表格记忆 |
| Smart-Memory | 分层 + 每层 token 预算条（提取链路需 ≥4B，需绕开） |
| RPG Companion + st-qdrant-memory | "世界状态不是故事"，JSON 精确存数值/任务 |
| MemU / Mem0 / Memobase | 记忆蒸馏思想（LLM 驱动，需改造） |

---

## 7. 后处理与安全

### 7.1 出戏关键词过滤

```text
我是AI / 作为语言模型 / 作为助手 / 抱歉，我不能 / 我不能回答 /
忽略之前的设定 / 系统提示 / prompt
```

命中 → 重试一次 → 仍失败则返回角色式兜底："你在说什么奇怪的话？"

### 7.2 长度控制

- 超长 → 只保留第一句 / 截断到 60 字。

---

## 8. 多角色方案

| 方案 | 优点 | 缺点 |
|---|---|---|
| 同一模型 + 角色卡切换 | 部署简单，一模型服务所有 NPC | 小模型可能角色混淆 |
| 不同 LoRA 对应角色 | 风格明显，不串角色 | 管理复杂，切换有工程成本 |

> 建议：先用方式一，发现主要角色易崩再单独训角色 LoRA。

---

## 9. 评估计划（不要只看 loss）

1. **角色一致性**：问身份/AI/年龄/来历，看是否保持角色。
2. **语气稳定性**：同角色多问不同问题，看语气是否一致。
3. **长度控制**：平均 15～60 字？是否常超 100 字？
4. **出戏率**：检测"作为AI/语言模型/我不能/抱歉/忽略之前设定"，越低越好。
5. **状态遵循率**：好感低应冷淡、好感高可亲近、任务未完不透露情报。

### 效果不达标时的排查优先级

```text
1. 数据质量 → 2. 输出长度控制 → 3. 角色卡是否太长 → 4. 是否加入状态信息
→ 5. 是否混入太多通用数据 → 6. 采样参数 → 7. 训练轮数 → 8. 模型是否太小
```

---

## 10. 混合方案（小模型仍崩时的备选）

- **方案 A**：关键剧情 NPC 用 0.5B/1.5B/3B，普通 NPC 用 MiniMind 或模板。
- **方案 B**：模板 + 小模型（普通回复用模板，复杂问题再生成）。
- **方案 C**：检索 + 小模型（先检索候选，模型改写语气）。

---

## 11. 风险清单

| 风险 | 应对 |
|---|---|
| 微调毁掉压缩带来的能力 | 优先 LoRA/QLoRA；全参后重走量化（QAT） |
| MoE 专家路由偏斜 | 保持任务多样性数据 |
| 上下文过载导致胡言乱语 | 记忆 token 预算纪律 + 截断 |
| 视觉编码器拖累内存/加载 | 纯文本场景剥离 vision tower |
| 引擎不支持模型结构 | 部署前先验证，必要时导 ONNX |
| 模型出戏 | 出戏防御样本 + 关键词过滤兜底 |
| 低端机延迟超预期 | 预生成 + 预取 + 流式 + 缓存 + 超时兜底 |

---

## 12. 交付物清单

- [ ] SFT 训练数据集（5000+ 条，含出戏防御/状态驱动样本）
- [ ] 微调后的 MiniMind 模型（LoRA 或全参，INT4 GGUF/MNN 导出）
- [ ] 角色卡模板（精炼版，关键信息拆短条件）
- [ ] 记忆系统（JSON 状态 + 关键词世界书 + FTS5/向量检索）
- [ ] 后处理模块（过滤/截断/兜底）
- [ ] 游戏接入 demo（单个 NPC 完整对话链路）
- [ ] 评估脚本（一致性/语气/长度/出戏率/状态遵循率）
- [ ] 端侧性能测试报告（四指标 + 真实机型）
- [ ] 基线评估报告（原始模型 vs 微调后，20 个常见意图）
